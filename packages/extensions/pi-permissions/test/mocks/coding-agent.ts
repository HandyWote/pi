// Type-only mock for @handy_wote/pi-coding-agent in vitest.
// The extension only uses types from the package at runtime (ExtensionAPI
// instances are passed in by the pi runtime), so no runtime exports are
// needed here.
export {};
