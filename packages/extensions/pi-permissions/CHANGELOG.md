# Changelog

## [Unreleased]

## [0.1.0] - 2026-08-02

### Added

- Added Claude Code-style security gate: permission modes (`chat`/`acceptEdits`/`auto`), rule engine with user/project/session rule sources and shadowing, tree-sitter bash analysis with subcommand splitting, sensitive-path redlines, AI classifier with fail-closed and denial limits, headless auto-deny with guidance, `/permissions` command with an interactive mode picker (EntityList dialog with left list / right descriptions, Enter to select, Escape to cancel, plus a View rules entry), CLI flags, and a denial audit log.
